import React from 'react';
import Header from './Header';
import Navbar from './Navbar';

const Home = () => {
    return (
        <div>
            <Navbar></Navbar>
            <Header></Header>
        </div>
    );
}

export default Home;
